/* Переменные */
:root{
    --main_font_size: 32px;
    --main_font: Helvetica;
}

* {
    margin: 0;
    padding: 0;
}



body {
    font-family: var(--main_font);
    font-size: var(--main_font_size);
    background-color: black;
}



.window {
    display: flex; /* динамическая расстановка объектов относительно друг друга внутри блока window*/
    flex-direction: row; /*внутри window расстановка по горизонтали*/
    height: 100vh;
}






/*/ IMAGES   /*/
/* левый bd чел */
.image_bd {
    margin-left: auto;
    margin-right: 0;
    height: 100vh;
}
/* правая картинка для окон помещения */
.image_interior {
    margin-right: auto;
    margin-left: 0;
    height: 100vh;
}

/* лицо в углу поверх всего */
.image_transparent_scary_face {
    display: block;
    position: absolute;
    width: 10%;
    opacity: 70%;
    top: 0; 
    left: 0;
}
/*/ ======== /*/







/*  TEXTS   */
.texts {
    display: flex;
    flex-direction: column; /*внутри texts расстановка по вертикали*/
    margin-left: 4%;
    margin-top: 15%;
}
.texts__text {
    color: white;
    margin-block: 5px;
    display: inline-flex;
    align-items: center;
}
.texts__logo {
    height: 1em;
    position: relative;
    top: 0.15em;
    opacity: 0; /*обычно скрываем иконку*/
    transition: opacity 0.5s ease; /*плавное исчезновение иконки и появление*/
}
.texts__text:hover .texts__logo { /*при наведении на ТЕКСТ(родиттеля элемента) включаем иконку*/
    opacity: 1;
}

.texts__textexp {
    color: white;
    margin-block: 10px;
}
.texts__logoexp {
    height: 15em;
    margin-top: 5px;
    opacity: 0; /*обычно скрываем иконку*/
    transition: opacity 0.5s ease; /*плавное исчезновение иконки и появление*/
}
.texts__textexp:hover .texts__logoexp { /*при наведении на ТЕКСТ(родиттеля элемента) включаем иконку*/
    opacity: 1;
}
